# Data

The employee-level dataset is embedded in the PBIX file and is not separately redistributed in this repository package. The `outputs` folder contains only aggregated results used to document the dashboard.

To refresh the Power BI report, obtain the original CSV, then update the source path in Power Query or replace it with a parameterized path.
